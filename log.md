# Changelog

## v1.16.0 — 2026-07-09

### 正解・不正解フィードバック演出を追加（音＋画面演出、全モード共通）
- **背景**: 正誤の手応えが指板上の色フィードバック（緑/赤/青）のみで、特に不正解時に「間違えた」ことが確実に伝わらないという指摘があった。音（正解＝明るいチャイム、不正解＝低いブザー）と画面演出（大きな○/✗オーバーレイ）を全モード（インターバル編・発見モード・アルペジオモード）共通で追加
- **設計判断**（事前にユーザー確認済み）:
  - アルペジオモード: 個々の正解タップでは演出せず、コード（シーケンス）を全部弾き終えた瞬間にのみ大きな○を表示（発見モードの「全構成音クリア」と同じ考え方に統一。当初の仕様書案は「1タップ＝1問クリア」だったが、実際の構造（1コード＝複数音を順番にタップ）と矛盾するため訂正）
  - 発見モードの`neutral`（既発見音の再タップ、青色フィードバック）: 音・演出とも出さない（既存の青色フィードバックのみ）
  - インターバル編のRootタップ正解（フェーズ1→2遷移）: 大きな○は出さない。大きな○は最終的な度数タップ正解時のみ
  - 不正解: 全モード・全ての不正解タップで毎回発火（インターバル編のRootフェーズ不正解も対象に追加。従来`onWrong`が呼ばれていなかった唯一の既存分岐）
  - 効果音の音色: ハンバーガーメニューの波形設定（sine/triangle/sawtooth/square）とは独立した固定音色（正解=sine系ベル、不正解=square系ブザー）
- **実装**: `js/audio.js`の`AudioEngine`に`playCorrectChime()`・`playWrongBuzz()`を追加（既存の`playNote`/`playChord`の音声トラッキングとは独立した自前のoscillator/gainを使用）。新規`js/feedbackFx.js`に画面演出専属クラス`FeedbackFx`を追加（`AudioEngine`=音、`Fretboard`=指板Canvasと同じ「1クラス1責務」の慣例）。`js/game.js`・`js/chordGame.js`・`js/arpeggioGame.js`の`handleTap()`に`onCorrect`/`onWrong`コールバック呼び出しを追加（既存の正誤判定基準自体は無変更）。`js/app.js`で`onCorrectFx`/`onWrongFx`ヘルパーを定義し3モードの`onCorrect`/`onWrong`に配線
- **UI**: `index.html`の`#screen-game`内に`#fx-overlay`（`position: fixed; pointer-events: none;`）を追加。○は0.5秒のふわっとしたフェード、✗は0.2〜0.3秒の短く鋭い出方＋画面シェイク。`pointer-events: none`により演出表示中も指板タップ操作を一切ブロックしない
- **Nodeモック検証**: `ChordGame`（300コード）・`ArpeggioGame`（300コード）・`Game`（Rootフェーズ不正解）で、`onCorrect`/`onWrong`が設計通りのタイミング（発見モード=全構成音クリア時のみ、neutralタップではonWrong不発火、アルペジオモード=シーケンス完了時のみ、インターバル編=Rootフェーズ不正解でもonWrong発火・Root正解ではonCorrect不発火）で発火することを確認
- **実機確認**: claude-in-chromeで3モードとも、実際のDOM（`#fx-overlay`）・音声モジュールに接続した並列ゲームインスタンスの`handleTap()`を直接呼び出し、○/✗演出が意図通りのタイミング・見た目で表示されることを確認（コンソールエラーなし）
- `sw.js` を `fretboard-v29` に更新

---

## v1.15.0 — 2026-07-09

### 出題コード範囲設定を追加（判定弦選択画面の拡張、発見モード・アルペジオモード共通）
- **背景**: コードトーン編にはインターバル編のようなLV階段が存在せず、練習モードが常に「全10コードタイプ＋テンションあり」という最難状態に固定されていた。判定弦選択画面（`#screen-judge-strings`）に出題コード範囲の設定を追加し、練習の難易度を選べるようにした。今回のプリセット区切りは将来のLV設計の土台になりうるが、LV機構自体の実装は今回のスコープ外
- **内部状態**: `{ chordTypeRangeAll: boolean, tensionEnabled: boolean }` の2軸で保持（`chordTypeRangeAll`=false: トライアドのみ[maj, min] / true: 全10種類。`tensionEnabled`はテンション出題の有無で発見モードのみ意味を持つ）
- **UI**: 判定弦選択画面に「出題コード範囲」プリセットボタン3つ（トライアドのみ／7th系まで／全部）と、独立した「テンション」ON/OFFトグルを追加。プリセットボタンは2軸への「ショートカット」であり、独立した3値の軸ではない。プリセットボタンクリックで対応する2軸を一括セットしてボタンをactive化。テンショントグルは常時表示・独立操作可能で、手動操作の結果がどのプリセットとも一致しない場合（例: トライアドのみ+テンションON）は全プリセットボタンを非active化（カスタム状態）。既存の波形選択ボタン（`.wave-btn`）と同じ「一致するボタンにだけactive」パターンを流用
- **アルペジオモードでのテンショントグル**: テンション非対応のため完全非表示（`hidden`クラスの付け外し）。既存の「開放弦」トグルがインターバル編専用でコードトーン編に存在しないのと同じ「そのモードに存在しない設定は出さない」パターンに揃えた
- **実装**: `js/music.js`に共有定数`TRIAD_TYPE_IDS`/`ALL_TYPE_IDS`を追加し、`js/chordGame.js`・`js/arpeggioGame.js`にハードコード重複していた`CHORD_TYPE_IDS`を置き換え。両クラスのコンストラクタで`chordTypeRangeAll`を受け取り`this._typeIds`を決定。`ChordGame`はさらに`tensionEnabled`を受け取り、`music.js`の`pickChordToneSet(typeId, type, tensionEnabled)`（新規引数、デフォルト`true`で後方互換）に渡す。`js/app.js`は`judgeStringStart`等と同じモジュールレベル変数として`chordTypeRangeAll`/`tensionEnabled`を管理し、発見モード・アルペジオモードで共有する
- **全数検証**: 「範囲（triad/all）×テンション（on/off）」の4通り × 有効な弦範囲10通り × 全12rootPc × 全ルート候補位置で`hasSolvableChordTones()`を総当たりし、全パターンで詰み0件を確認（triad+OFF: 1,440通り、triad+ON: 3,600通り、all+OFF: 7,200通り、all+ON: 18,000通り）
- **モック検証**: 各設定で`ChordGame`/`ArpeggioGame`の`_nextChord()`を1000回実行し、出題されるコードタイプ・テンション有無が設定と一致することを確認
- **実機確認**: プリセットボタンのactive/カスタム状態切り替え、発見モード⇄アルペジオモード間の設定共有（弦数・範囲）、アルペジオモードでのテンショントグル非表示、トライアドのみ+テンションONのカスタム状態での実際の出題（例:「C(13)」4音構成）を確認
- **スコープ外**: LV概念そのものの設計、アルペジオモードの部分集合選択・出題順序制約・一発勝負化は今回持ち越し。インターバル編（`js/game.js`）は無変更
- `sw.js` を `fretboard-v28` に更新

---

## v1.14.0 — 2026-07-08

### コードトーン編〔アルペジオモード〕に7th系コードタイプ対応を追加（maj/min 2種 → 全10種）
- **背景**: 発見モード（`js/chordGame.js`）はStage 5（v1.10.0）で既に全10コードタイプに対応済みだったが、アルペジオモード（`js/arpeggioGame.js`）はトライアド（maj/min）2種類のみに留まっていた。発見モードStage 5で確立したパターン（コードタイプ数を増やしても`_nextChord()`の動的リトライ・可解性チェックは無改修で対応できる設計）をアルペジオモードに横展開した
- **実装**: `CHORD_TYPE_IDS`を`['maj', 'min']`から全10種類（maj, min, maj7, min7, dom7, dim7, m7b5, aug, sus2, sus4）に拡大。`_nextChord()`のdo-whileリトライ・`hasSolvableChordTones()`呼び出しは想定通り無改修で対応できた（`type.chord`という汎用的な半音配列を渡すだけの構造だったため）
- **コード名表記の変更**: 発見モード（v1.13.0〜）と同じ記号表記（`type.symbol`、例:「C7」「C#dim7」）に統一。従来の英語フル表記（`type.name`、例:「C Minor」）のままだと7th系拡大で「C# Diminished 7」のように長くなり画面崩れのリスクがあったため。アルペジオモードはテンション非対応のため括弧表記は付けない
- **テンション対応・出題度数の部分集合選択・不正解時の挙動変更は今回のスコープ外**（別途持ち越し、詳細はCLAUDE.md参照）
- **全数検証**: 有効な弦範囲10通り×全12rootPc×全ルート候補位置×全10コードタイプ（基本構成音のみ、テンションなし、合計7,200通り）で`hasSolvableChordTones()`を総当たりし、詰み0件を確認
- **モック検証**: `ArpeggioGame._nextChord()`を3000回実行し、トライアド系（maj/min/aug/sus2/sus4=3音）と7th系（maj7/min7/dom7/dim7/m7b5=4音）の出現比率が期待通り約5:5であること、全120パターン（12ルート×10タイプ）のコード名が正しい記号表記で生成されること、構成音のピッチクラス重複が発生しないことを確認
- **スコープ外**: 発見モード（`js/chordGame.js`）・インターバル編（`js/game.js`）は無変更
- `sw.js` を `fretboard-v27` に更新

---

## v1.13.1 — 2026-07-08

### CHORD_TYPES.tensionsデータ見直し（明白なアボイドノートのみ除外）
- **背景**: v1.13.0のコードレビュー（INFO-4）で、`CHORD_TYPES.tensions`の一部に音楽理論上のアボイドノート（コードトーンの半音上でぶつかる音）が含まれている指摘があった。運指・ポジション感覚を養うというアプリの目的を踏まえ、理論的厳密さを追求しすぎず、実用的な基準で判断する方針とした
- **判断基準**: 「コードトーンに対して半音上（短9度上）でぶつかる、文脈（キーやダイアトニックの何段目か）に関係なくほぼ常にアボイドと言える明白なケースのみ」を除外対象とした。b9・#9のような変化音（ルート等を意図的に半音ずらした音）は、その定義自体が意図的に半音の位置に置かれる性質のテンション（アボイドの回避策として使われるもの）であり、除外対象の判定基準からは除いた
- **除外したテンション**:
  - `maj`の`11th`: P4が3rd（長3度）の半音上でぶつかる明白なアボイドノート
  - `dom7`の`11th`: majの11thと全く同じ構造（長3度の半音上）。ジャズ理論で「V7のP11はアボイド」として頻出する代表例
- **除外後のtensions**: `maj: { '9th', '13th' }`（2択）、`dom7: { 'b9', '9th', '#9', '#11th', '13th' }`（5択、11thのみ削除）
- **グレーゾーンとして現状維持したもの**: `min`の`b13`はP5（完全5度）の半音上にあり機械的にはmajの11thと類似の「半音上でぶつかる」パターンに該当する。ただし、b9・#9とは異なりb13は変化音ではなく、自然短音階（Aeolian）にもともと含まれるダイアトニックな音（呼称が「b13」なので変化音の仲間に見えるだけ）。標準的な理論書（Berklee系のavailable tensions表等）ではマイナーコード上のb13はむしろ5thとの衝突ゆえアボイドノートに分類されることが多く、「実務で頻用されるavailable tensionだから残す」という単純な理由付けは必ずしも整合しない。それでも、film/rockなどで「マイナー♭6」という響き自体が独自の色として広く認識・使用されている実務上の事実を踏まえ、majの11thほど普遍的なアボイドではなく、マイナー♭6という認知された色として単独提示する価値があると判断し、厳密な理論では避けられうる点は承知の上であえて残した
- **他コードタイプの確認結果（該当なし）**: maj7・min7・dim7・m7b5・augは全テンションがコードトーンと半音接触なし、または半音"下"（逆方向、アボイドの典型パターンではない）、または変化テンション（除外対象基準外）のいずれかで、除外対象なし。sus2は既存のピッチクラス衝突ロジック（2ndとの重複）をそのまま維持
- **実装**: `js/music.js`の`CHORD_TYPES.maj.tensions`・`CHORD_TYPES.dom7.tensions`の2箇所のみ変更。`js/chordGame.js`・`js/arpeggioGame.js`・`js/game.js`は出題ロジック自体を変更していないため無変更
- **全数検証**: 有効な弦範囲10通り×全12rootPc×全ルート候補位置×全10コードタイプの全テンションパターン（18,000通り、v1.13.0の19,440通りより減少）で`hasSolvableChordTones()`を総当たりし、詰み0件を確認
- **モック検証**: `ChordGame._nextChord()`を5000回実行し、maj・dom7で`(11)`が一切出現しないこと（0回）、minの`(11)`・`(b13)`は引き続き正常に出現すること（無変更を確認）、pcの重複が発生しないことを確認
- `sw.js` を `fretboard-v26` に更新

---

## v1.13.0 — 2026-07-08

### コードトーン編〔発見モード〕テンション出題方式を「1個ランダム選択」に再設計、コード名表記も記号化
- **背景**: v1.12.0（Stage 6）で「定義済み全テンションをまとめて出題」する形で実装したが、実機確認の結果、表示されるコード名（例:「C Dominant 7th」）が本来4音を指す名前であるのに対し、実際に要求される構成音は最大10音（dom7）に達しており、コード名と出題内容が一致しないというコンセプト上の矛盾が見つかった
- **方針転換**: ジャズ理論の「アベイラブルテンション」の考え方（コード上で使用可能なオプションの音であり、実際の演奏でも全部同時に使うことはまずなく、その場で1つか2つを選んで色付けするのが通常）を踏まえ、テンションの出題を「定義済みテンション配列から毎回1個だけランダム選択」に変更した
- **コード名表記の変更**: `CHORD_TYPES`の各エントリに`symbol`フィールド（例: dom7→`'7'`、maj7→`'maj7'`、min→`'m'`）を追加し、コード名を英語フル表記（`type.name`、例:「Dominant 7th」）から一般的な記号表記に変更。選ばれたテンションは括弧で明示し（例:「C7(9)」「Cmaj7(#11)」「Csus4(9)」）、表示されたコード名と実際に要求される構成音を常に一致させた。テンション名の括弧内表記は末尾の「th」を除去する変換（`'9th'`→`'9'`、`'b9'`/`'#9'`/`'b13'`は元々「th」を含まないためそのまま）
- **sus2の扱い**: 唯一の定義済みテンション9thが基本構成音2ndとピッチクラス衝突し実質空振りになるため、テンション抽選の対象から明示的に除外（常にテンションなしの3音固定で出題）
- **実装**: `music.js`の`extendedChordTones(type)`（全テンション統合）を`pickChordToneSet(typeId, type)`に置き換え、`type.tensions`から1件をランダム選択して返す（戻り値は`{ tones, tensionName }`）。sus2の場合は`typeId`で判定してテンション選択自体をスキップ。`hasSolvableChordTones()`は内部実装が汎用的なため無変更。`chordGame.js`の`_nextChord()`内、可解性チェック呼び出し・構成音リスト生成・コード名生成の3箇所を変更（`handleTap()`・`_emitProgress()`・`_playChord()`は`_chordTones`配列への汎用ロジックのため無変更）
- **拡張後の実質音数**: sus2=3音（テンションなし固定）、トライアド系（maj/min/aug/sus4）=4音（基本3音+テンション1）、7th系（maj7/min7/dom7/dim7/m7b5）=5音（基本4音+テンション1）
- **全数検証**: 有効な弦範囲10通り × 全12rootPc × 全ルート候補位置 × 全10コードタイプの全テンションパターン（sus2はテンションなしの1パターンのみ、他9種類は定義済みテンション全パターン、合計19,440通り）で`hasSolvableChordTones()`を総当たりし、詰み0件を確認
- **モック検証**: `ChordGame._nextChord()`を5000回実行し、pcの重複が発生しないこと、コード名の生成パターンが常に正しい形式（例:「C7(9)」「Csus2」）であること、実質音数の分布（3音≒1/10・4音≒4/10・5音≒5/10）が期待通りであることを確認
- **スコープ外**: アルペジオモード（`js/arpeggioGame.js`）・インターバル編（`js/game.js`）は無変更
- `sw.js` を `fretboard-v25` に更新

---

## v1.12.0 — 2026-07-07

### コードトーン編〔発見モード〕にテンション対応を追加（Stage 6）
- **背景**: 前回の申し送り調査で、発見モードにはテンション判定ロジック自体が実は未実装（`CHORD_TYPES.tensions`は定義のみでどこからも参照されない死んだデータ）と判明。方針として「コードトーン編はオクターブ無視・ピッチクラスベースが正しい設計」（鳴っているコードの中でどの音を弾くかという演奏上の問いであり、基準音との距離を厳密に問うインターバル編とは判定基準が別物）が確定したため、この方針でゼロからテンション対応を実装した
- **出題内容**: 各コードタイプに定義済みの全テンション（9th・b9・#9・11th・#11th・13th・b13）を基本構成音とあわせて毎回まとめて出題する（1個をランダム抽選するのではなく定義済み全部）。オクターブは無視し、ピッチクラス一致のみで正解とする
- **実装**: `music.js`に新規関数`extendedChordTones(type)`を追加。`type.chord`（基本構成音）と`type.tensions`（テンション）を統合し、ピッチクラスが重複するものを除外した拡張構成音リストを生成する。`semitone`は生の値（mod12化しない）のまま保持し、`_playChord()`でルートMIDIに加算する際に実際に正しいオクターブ（テンションは基準音より1オクターブ以上高い位置）で鳴らせるようにしている
- **既存コードへの影響は最小限**: `hasSolvableChordTones()`（`music.js`）は内部で`(rootPc + semitone) % 12`を計算する既存実装のままで12以上の生の半音数（テンション）も正しく扱えるため無変更。`chordGame.js`の`handleTap()`・`_emitProgress()`・`_playChord()`もいずれも`_chordTones`配列に対して汎用的に動作するため無変更。変更したのは`_nextChord()`内の可解性チェック呼び出しと構成音リスト生成の2箇所のみ
- **sus2のピッチクラス衝突**: sus2の基本構成音2nd（pc2）と、定義済みテンション9th（14半音→pc2）が完全に一致する唯一のケース。ピッチクラス基準で重複除去する方針を採用し、`extendedChordTones()`の重複除去ロジックを全コードタイプ共通の汎用ルールとして実装（sus2を特別扱いしない）。結果としてsus2は実質R・2nd・5thの3音のまま出題され、9thは2ndに吸収され進捗表示にも出ない。他の9コードタイプは基本構成音とテンションのpcが一切衝突しない
- **拡張後の実質音数**: maj/min/aug=6音、maj7/min7/m7b5=7音、dom7=10音（最多）、dim7=5音、sus2=3音（最少）、sus4=4音
- **全数検証**: 有効な弦範囲10通り（本数3〜6）× 全12rootPc × 全ルート候補位置 × 全10コードタイプの拡張構成音セット（合計7200通り）で`hasSolvableChordTones()`を総当たりし、詰み0件を確認（dom7の10音・判定弦3本の組み合わせも含む）
- **モック検証**: Audio/Fretboardをモック化した`ChordGame._nextChord()`を5000回実行し、コードタイプごとの音数が常に一定（sus2=3, dom7=10等）であること、`_chordTones`内でpcの重複が発生しないことを確認
- **スコープ外**: アルペジオモード（`js/arpeggioGame.js`）は7th系自体が未対応のため、テンション対応は別課題として引き続き切り離す。インターバル編（`js/game.js`）のオクターブ厳格化も別課題のまま無変更
- `sw.js` を `fretboard-v24` に更新

---

## v1.11.1 — 2026-07-06

### 判定弦カスタマイズ機能のUIを全画面デュアルスライダーに変更
- **背景**: v1.11.0でハンバーガーメニュー内に実装した「3本/4本/5本/6本」＋「6弦〜1弦」の2段ボタンUIが、実際に触ると直感的にわかりにくいことが判明。判定弦域を選ぶという操作の性質上、範囲を視覚的にドラッグで選べるデュアルハンドルスライダーの方が理解しやすいと判断し、UIを作り直した
- **画面遷移の変更**: 発見モード・アルペジオモードの「練習をはじめる」タップ時、これまで即座に練習を開始していたが、新設の「判定弦を選ぶ」全画面（`#screen-judge-strings`）を経由するように変更。スライダーで範囲を選んで「この設定で練習をはじめる」を押すと practice が開始される。設定場所をハンバーガーメニュー（設定全般）から練習開始直前の専用画面に移した
- **スライダー実装**: 2本の`<input type="range" min="0" max="5" step="1">`を同一コンテナに重ねる定番の「デュアルレンジスライダー」方式を採用。それぞれのtrackを透明化し、つまみ（`::-webkit-slider-thumb`/`::-moz-range-thumb`）のみ`pointer-events: auto`にすることで、track部分の誤タップで意図しない側が反応する問題を回避。目盛りは6・5・4・3・2・1（左が低音・右が高音）、選択範囲は`var(--accent)`（既存のオレンジ系アクセントカラー）で塗った帯で視覚化した（参考画像は赤だったが、既存デザインとの統一感を優先しオレンジに変更）
- **最小本数制約**: 開始側・終了側どちらの`input`イベントでも、ギャップが2未満（＝3本未満）になる場合はもう片方のつまみを連動して押し出すロジックを実装。0〜5の6段階・最小ギャップ2という制約上、2つのつまみが重なる／すれ違う事態は構造的に発生しない
- **データモデル・検証ロジックは無変更**: `judgeStringStart`/`judgeStringCount`/`computeStringRange()`（`js/app.js`）、`stringRange`を受け取る`js/chordGame.js`・`js/arpeggioGame.js`のコンストラクタ、`js/music.js`の`hasSolvableChordTones()`/`rootPositionCandidates()`はv1.11.0のまま完全に無変更。今回はそれを呼び出す側のUIのみを作り直した
- ハンバーガーメニューから旧UI（`#judge-count-btns`・`#judge-start-btns`・関連イベントリスナー・`.wave-btn:disabled`のCSS）を削除
- 実機相当のブラウザ確認で、スライダーのドラッグ操作・最小本数制約による連動・選択結果のマスク表示とルート出現弦への反映（発見モード・アルペジオモード両方）・インターバル編への無回帰・ハンバーガーメニューの表示崩れがないことを確認
- `sw.js` を `fretboard-v23` に更新

---

## v1.11.0 — 2026-07-06

### コードトーン編（発見モード・アルペジオモード共通）に判定弦カスタマイズ機能を追加
- **背景**: コードトーン編（発見モード・アルペジオモード）は判定対象弦が低音3弦（6〜4弦）に固定されていた。ユーザーが自由に設定できるようにする
- **事前調査**: 実装前の調査で、`js/fretboard.js` のマスク描画（`draw({ maskStrings })`）と `js/music.js` の `hasSolvableChordTones()` / `rootPositionCandidates()` はいずれも既に任意の弦範囲を引数で受け取れる汎用実装であることを確認（改修不要）。一方で `ROOT_STRINGS` と `JUDGE_STRINGS` は `chordGame.js`・`arpeggioGame.js` それぞれで独立に定義された別々の定数で、値がたまたま `[0,1,2]` で一致していただけと判明。`hasSolvableChordTones()` はルートの物理弦位置を見ておらず、構成音のピッチクラスが判定弦域に存在するかだけを見ている
- **設計判断**: この調査結果を踏まえ、`ROOT_STRINGS` をユーザー設定の判定弦域と統合（同じ値を共有する形に変更）。「選んだ弦域だけで練習が完結する」体験を優先し、判定弦を変更するとルート音の出現弦も連動して変わるようにした
- **データモデル**: 判定弦域を「開始弦インデックス（0=6弦〜5=1弦）＋本数（3〜6）」で表現。`js/chordGame.js`・`js/arpeggioGame.js` はモジュールレベルの固定定数 `ROOT_STRINGS`/`JUDGE_STRINGS` を廃止し、コンストラクタ引数 `stringRange` を `this._stringRange` として保持する形に変更。両ファイルの `_nextChord()`・`handleTap()`・マスク生成・`_pickRootPosition()` のフォールバックが参照する箇所をすべて `this._stringRange` に統一
- **UI**: ハンバーガーメニューに「コードトーン編：判定弦」設定を追加。本数ボタン（3〜6本）と開始弦ボタン（6弦〜1弦）の2段構成で、本数変更時に無効な組み合わせとなる開始弦ボタンを自動的にdisabledにする。発見モード・アルペジオモードで共通の1つの設定（`js/app.js` の `judgeStringStart`/`judgeStringCount`）。設定変更の反映タイミングは既存の `hintEnabled` 等と同じく「次に練習をはじめたとき」
- **全数検証**: 実装後、Node上で有効な弦範囲の組み合わせ全10通り（本数3: 開始弦0-3の4通り、本数4: 0-2の3通り、本数5: 0-1の2通り、本数6: 0のみ）× 全12rootPc × 全ルート候補位置 × 全10コードタイプ（7200通り）で `hasSolvableChordTones()` を総当たりし、詰み0件を確認。特に本数3の全4パターン（低音3弦〜高音3弦）× 7th系4音組を重点的に確認
- **モック検証**: デフォルト設定（6〜4弦）・高音3弦（3〜1弦）・全弦（6本）の3パターンで `ChordGame._nextChord()` / `ArpeggioGame._nextChord()` を各2000回実行し、指定弦域内でのみrootPc・弦・オクターブ・コードタイプが偏りなく出現し詰みが発生しないことを確認
- **実機相当のブラウザ確認**: 発見モード・アルペジオモード両方で、判定弦設定を変更した際にマスク表示（ハイライト位置の反転）・タップ判定・実際に鳴るルート音の弦が正しく連動することを確認。インターバル編（無変更）への回帰がないことも確認
- **ドキュメント整合性の副次修正**: `README.md` のアルペジオモード節が、v1.8.0（Stage 3, ルート可変化）実装後もStage 2表記・「ルートC固定」のまま更新されていなかった記述の齟齬を今回合わせて修正
- `sw.js` を `fretboard-v22` に更新

---

## v1.10.0 — 2026-07-06

### コードトーン編〔発見モード〕の7th系8種類を復帰し全10種類対応を完了（Stage 5）
- **背景**: Stage 4（v1.9.0）でルート音を12音フルランダム化した際、切り分けを容易にするため対象コードタイプを一時的に maj/min トライアドのみへ絞り込んでいた。Stage 4のコードレビュー（`CODE_REVIEW_2026-07-03_chordgame-stage4.md`）のF1申し送りに対応し、7th系8種類（maj7, min7, dom7, dim7, m7b5, aug, sus2, sus4）を復帰する
- **変更点**: `js/chordGame.js` の `CHORD_TYPE_IDS` を `['maj', 'min']` から全10種類（`music.js` の `CHORD_TYPES` 全キー）に復帰。`_nextChord()` のdo-whileリトライ（`hasSolvableChordTones()` による判定、最大30回）はコードタイプ非依存の設計のため変更不要。`music.js`・`js/arpeggioGame.js`・`js/game.js` は無変更
- **全数検証（F1対応）**: 実装前にNode上で、全12 rootPc × 全ルート候補位置（`rootPositionCandidates()` が返す6/5/4弦×0/1オクターブの候補、計540通り）× 全10コードタイプの直積で `hasSolvableChordTones()` を総当たり実行。3音組（maj, min, sus2, sus4, aug: 270通り）・4音組（maj7, min7, dom7, dim7, m7b5: 270通り）とも詰み0件を確認
- **モック検証**: `ChordGame._nextChord()` をAudio/Fretboardモック経由で5000回呼び出し、全12rootPc・6/5/4弦（ほぼ均等）・両オクターブ・全10コードタイプが偏りなく出現し、詰みが一度も発生しないことを確認
- **任意対応（N1nit）**: `_pickRootPosition()` のフォールバックのフレット探索上限を `f = 0..12` → `f = 0..11` に統一し、`music.js` の `rootPositionCandidates()` と揃えた（フォールバック自体は候補ゼロ時のデッドコードのため実害なし）。`ArpeggioGame._pickRootPosition()` は今回のスコープ外のため未修正
- **次ステージへの申し送り**: 発見モードのルート可変化・全10種類対応はこれで完了。次はテンション対応を検討するが、着手前に「オクターブ無視ルールがテンション判定を無効化していないか」という概念的な疑問（発見モードStage 1のテンション実装・インターバル編LV.4/LV.Maxを含めて未検証）を必ず解消すること
- `sw.js` を `fretboard-v21` に更新

---

## v1.9.0 — 2026-07-03

### コードトーン編〔発見モード〕のルート音をランダム化（Stage 4）
- **背景**: 発見モードはStage 1でルートC固定・低音3弦固定だった。アルペジオモードStage 3（v1.8.0）で実装・検証済みのルート可変化の仕組み（`music.js` の共有関数 `rootPositionCandidates()` ＋ 動的do-whileリトライ）を発見モードへ横展開する
- **範囲**: アルペジオStage 3と同じインターバル編LV.4方式。ルートピッチクラスを12音フルランダム化し、弦・オクターブも `ROOT_STRINGS: [0,1,2]`（6/5/4弦）・`ROOT_OCTAVES: [0,1]` の組み合わせからランダム選択。判定対象弦（低音3弦）はルート位置とは独立に固定のまま変更していない
- **対象コードタイプを一時的に maj/min トライアドのみへ絞り込み**: ルート可変化という新しい変数を導入するにあたり、原因切り分けを容易にするため（Stage 2でアルペジオモードをmaj/minから始めたのと同じ考え方）。`music.js` の `CHORD_TYPES` データ構造（全10種類）は無変更で、`js/chordGame.js` 側の `CHORD_TYPE_IDS` を `['maj','min']` に絞るだけにし、7th系8種類はコメントで残して次ステージで戻しやすくした
- **`js/chordGame.js` の `_nextChord()` を静的1回チェックから動的リトライへ変更**: `Game._nextQuestion()` / `ArpeggioGame._nextChord()` と同型の「ルートPC・ルートポジション・コードタイプをまとめて再抽選するdo-whileリトライ」（最大30回、`MAX_TYPE_RETRY` → `MAX_QUESTION_RETRY` にリネーム）。`_pickRootPosition()` を新設（`rootPositionCandidates()` 呼び出し＋候補ゼロ時フォールバック、`ArpeggioGame` と同一）。`music.js`・`js/arpeggioGame.js`・`js/game.js` は無変更
- **判定ロジック（未クリア集合方式）は無変更**: ピッチクラス比較のみでルート値に依存しないため、ルート可変化の影響を受けない
- Node上でモック（Audio/Fretboardをモック化）による検証スクリプトで `_nextChord()` を5000回呼び出し、可解性チェックが常に成立（詰みゼロ）・ルートPCが12音全てに分布・ルート弦が6/5/4弦にほぼ均等（各33%前後）分布・両オクターブ出現・抽選タイプがmaj/minの2種のみ・判定対象弦以外が正しくマスクされることを機械的に確認。アルペジオモード・インターバル編への回帰がないことも確認
- **将来のテンション対応時に要確認の申し送り**（v1.8.0から継続）: 「オクターブ無視ルールがテンション判定を無効化していないか」という概念的な疑問は未解決。テンション対応に着手する前に、発見モード・アルペジオモード・インターバル編（`js/game.js` のLV.4/LV.Max該当箇所）を含めて必ず確認すること
- `sw.js` を `fretboard-v20` に更新

---

## v1.8.1 — 2026-07-03

### GitHub Pages のデプロイ方式を GitHub Actions ベースへ移行
- **背景**: 従来はレガシー方式（`pages-build-deployment`、GitHubが自動生成する特殊ワークフロー）でデプロイしていたが、`gh run rerun` と相性が悪く queued のまま進まなくなる問題があった（Stage 3 / v1.8.0 デプロイ時に発生）。実際、切り替え作業中にもレガシー実行が33分以上 queued で停滞している痕跡を確認
- **対応**: `.github/workflows/deploy-pages.yml` を新規追加。ビルド工程不要な静的サイト向けの標準構成（`actions/checkout` → `actions/configure-pages` → `actions/upload-pages-artifact`（`path: .`）→ `actions/deploy-pages`）。トリガーは main への push（＋手動実行用に `workflow_dispatch`）。`permissions`（`pages: write` / `id-token: write`）と `concurrency: pages`（`cancel-in-progress: false`）を設定
- **切り替え手順**: ワークフロー追加後、`Settings → Pages → Source` を「GitHub Actions」へ手動変更（`build_type` が `legacy` → `workflow` に変わる）。切り替え前にワークフローが一度デプロイ成功済みだったためダウンタイムなしで移行。切り替え後はレガシーの自動ビルドが停止し、Actions ワークフロー単独で走ることを本コミットのデプロイで確認
- アプリ本体のロジックは無変更（バージョン番号のみ更新）。`sw.js` を `fretboard-v19` に更新

---

## v1.8.0 — 2026-07-03

### コードトーン編〔アルペジオモード〕のルート音をランダム化（Stage 3）
- **背景**: Stage 2で実装したアルペジオモードはルートC固定だった。発見モード（全10コードタイプ対応済み）より対象コードタイプが少なく検証範囲が狭いアルペジオモードを先にルート可変化することで、不具合発生時の切り分けを容易にする狙い
- **範囲**: インターバル編LV.4方式を採用。ルートピッチクラスを12音フルランダム化し、弦・オクターブも `rootStrings: [0,1,2]`（6/5/4弦）・`rootOctaves: [0,1]` の組み合わせからランダム選択。判定対象弦（低音3弦）はルート位置とは独立に固定のまま変更していない
- **`js/music.js` に共有関数 `rootPositionCandidates(rootPc, rootStrings, rootOctaves)` を新設**: `Game._pickRootPosition`（`js/game.js`）と同じ考え方でルートポジション候補を列挙する関数。候補抽選・候補ゼロ時のフォールバックは呼び出し側の責務とし、関数自体は候補配列を返すだけのシンプルな形にした。`Game._pickRootPosition` 自体は今回リファクタしていない（スコープ外）
- **`js/arpeggioGame.js` の `_nextChord()` を静的1回チェックから動的リトライへ変更**: Stage 2ではルート固定のためコードタイプのみをリトライ対象にしていたが、`Game._nextQuestion()` と同型の「ルートPC・ルートポジション・コードタイプをまとめて再抽選するdo-whileリトライ」（最大30回、`MAX_QUESTION_RETRY`にリネーム）に変更。`hasSolvableChordTones()` 自体は変更不要で、呼び出し側で毎回異なる `rootPc`/`rootFret` を渡すだけで対応できた
- Node上でモック（Audio/Fretboardをモック化）による検証スクリプトで、`_nextChord()` を2000回呼び出し可解性チェックが常に成立すること（詰みが発生しないこと）、ルートPCが12音全てに分布することを確認。実機相当のブラウザ確認でも、ルート・フレット位置が毎回変わりつつストローク→アルペジオ再生→タップ判定・ヒント表示・次のコードへの遷移が正しく動作すること、発見モード（`js/chordGame.js`、無変更）・インターバル編（`js/game.js`、無変更）への回帰がないことを確認
- **将来のテンション対応時に要確認の申し送り**: 「オクターブ無視ルールがテンション判定を無効化していないか」という概念的な疑問が未解決。発見モードのStage 1テンション実装（9th/b9のみ可解）がこの区別を正しく実装できているか、インターバル編（`js/game.js` のLV.4/LV.Max該当箇所）も含めて、テンション対応に着手する前に必ず確認すること
- `sw.js` を `fretboard-v18` に更新

---

## v1.7.0 — 2026-07-02

### コードトーン編にアルペジオモードを新規追加（Stage 2）
- **背景**: 発見モード（Stage 0/1、未クリア集合方式）は「コードの中に何があるか（構成音の同定）」を鍛える練習だった。アプリの核心コンセプト「耳と手の直結」をさらに深めるため、「耳で掴んだコード感の上に、頭でメロディをイメージし、それを指板上で再現する」練習として新モード「アルペジオモード」を追加。実際の演奏の「ストロークでコード感を掴む→アルペジオ的にメロディを紡ぐ」という2層構造を再現する狙い
- **新規ファイル `js/arpeggioGame.js`（`ArpeggioGame`クラス）**: `game.js↔Game`・`chordGame.js↔ChordGame`と同じ「1クラス1ファイル」の慣例に沿って分離。`js/chordGame.js`（発見モード）は1行も変更していない
- **出題**: ルートC固定・低音3弦・トライアド（maj/min）のみ対象（7th系は対象外）。`hasSolvableChordTones()`をそのまま流用し、出題可解性は発見モードと同じ条件でチェック
- **出題の流れ**: ①コードをストローク再生 → ②構成音をランダムな順序でアルペジオ再生 → ③再生完了後、聞こえた順番どおりにタップ。再生中（合計2〜3秒程度）はタップを無視する（度数名を隠して音順記憶を鍛える本モードの目的上、再生中のタップを許すと「聴かずに色フィードバックの試行錯誤で解く」抜け道が成立してしまうため、インターバル編とは意図的に異なる挙動にした）
- **判定ロジック（フェーズ管理方式）**: インターバル編と同型。「今何番目の音を待っているか」のポインタを持ち、正しいピッチクラスのタップで次へ進める。構成音でも順番違いのタップは通常の不正解と同じ扱い（赤一色、発見モードの中立色`'neutral'`は使わない）。不正解時は同じフェーズのまま待機する「粘れる」方式
- **アルペジオ再生の自前スケジューリング**: `audio.js`の未使用スキャフォールド`playArpeggio()`は使わず（呼び出し元からキャンセルする手段がなく画面離脱時に音が止まらない潜在バグを持つため）、`ArpeggioGame`側で`setTimeout`を自前管理し`stop()`/`replay()`で確実にキャンセルできるようにした
- **進捗表示**: 度数名を隠し「1音目 ✓　2音目 ✓　3音目（待機中）」のように順番のみ表示。発見モードと同じ`body.mode-chord`の2行レイアウトCSSをそのまま流用（追加CSS不要）
- **もう一度・ヒント機能**: 「もう一度」はフェーズ進捗を保持したままストローク＋アルペジオを再生し直す。ヒント機能はインターバル編の`Game._scheduleHint()`と同型（スケジュール時点のフェーズを覚えておき、発火時に不一致なら無視するガード）で実装
- **ホーム画面**: 3モード目として追加（インターバル編／コードトーン編〔発見〕／コードトーン編〔アルペジオ〕）。`selectMode()`を2値から3値判定に変更
- Node上でAudio/Fretboardをモックにした検証スクリプトで、再生ロック・正しい順序でのタップ・順番違いのタップ・もう一度でのフェーズ保持・ヒントのフェーズガードを機械的に確認。実機（Chrome）でも3モードの切り替え・2行レイアウト・発見モード/インターバル編への回帰がないことを確認
- `sw.js` を `fretboard-v17` に更新（`js/arpeggioGame.js` を `ASSETS` に追加）

---

## v1.6.2 — 2026-07-02

### hasSolvableChordTones()のチェック範囲をGame._hasValidAnswerと同型に修正
- **背景**: 別セッションでのコードレビューにより、`js/music.js` の `hasSolvableChordTones()` のチェック下限フレットが、実際にタップ判定を行っている `fretboard.js` / `game.js` の `_hasValidAnswer` より低音側に1フレット広いことが判明。`calcDisplayRange` の `start` は指板左端の「フレット線」位置でありタップ不可のゾーンのため、本来のループ範囲は `start+1〜end+1`
- **修正**: `hasSolvableChordTones()` のループ下限を `start` → `start+1` に変更。あわせて `_hasValidAnswer` にある「`start === 0` の場合のみ開放弦(0F)を許可する」分岐を追加（ChordGameには `_excludeOpenStrings` 相当の設定が存在しないため、その条件は含めていない）
- Stage 1（ルートC固定・全10種類）は `start !== 0` のため実害はなかった（修正前は `start` のフレット1本分が余分にヒット対象になっていただけで、たまたま可解性の判定結果は変わらなかった）が、将来ルート可変化時に `start === 0` のケースが発生するとバグ化するため先行修正
- Node上で修正後も全10種類（maj, min, maj7, min7, dom7, dim7, m7b5, aug, sus2, sus4）が引き続き可解であることを回帰確認。実機でも動作・2行レイアウトに影響がないことを確認
- `sw.js` を `fretboard-v16` に更新

---

## v1.6.1 — 2026-07-02

### コードトーン編の進捗表示を2行レイアウトに調整
- **背景**: v1.6.0（Stage 1）の実機確認で動作自体は問題なかったが、コード名と構成音の進捗テキストが1行表示のままだと、今後コードタイプ・テンションがさらに増えた際に横幅が厳しくなる懸念があったため、先行して2行表示に変更
- **対応**: `root-name`/`interval-name`はインターバル編（Gameクラス）とコードトーン編（ChordGameクラス）で共用の要素のため、CSSだけではコードトーン編のみを2行化できなかった。`js/app.js` の `startChordPractice()`/`startGame()` で `body.mode-chord` クラスをトグルし、`css/style.css` に `body.mode-chord .question-area { flex-direction: column-reverse; }` を追加してコードトーン編限定で2行化（上段: コード名、下段: 進捗テキスト）。DOM順序・テキスト代入ロジックは変更していない
- インターバル編（横並びのフェーズガイド表示）への影響なしを確認
- `sw.js` を `fretboard-v15` に更新

---

## v1.6.0 — 2026-07-02

### コードトーン編 Stage 1: コードタイプを7th系まで拡張
- **出題コードタイプを2種類→10種類に拡張**: メジャー/マイナートライアドに加え、Major 7th・Minor 7th・Dominant 7th・Diminished 7・Half Diminished（m7b5）・Augmented・Sus2・Sus4 を追加（`js/chordGame.js` の `CHORD_TYPE_IDS`）。ルート音の可変化は対象外（Cのまま据え置き）、テンション（9th/11th等）も対象外
- **出題可解性チェックを追加**: `js/music.js` に `hasSolvableChordTones(rootPc, rootFret, judgeStrings, semitones)` を新設。ルート×コードタイプの組み合わせ単位で、低音3弦・6フレット窓に全構成音が収まるか判定する。`ChordGame._nextChord()` のタイプ抽選を `Game._hasValidAnswer` と同型の再試行パターン（最大30回）に変更し、Stage 0で申し送りだった「出題可解性検証の欠如」を解消
- **可解性の事前検証**: 実装前にNode上で実際の `music.js` をインポートし、`CHORD_TYPES` 全10種類が低音3弦・6フレット窓・ルートC固定の条件下で機械的に可解であることを確認済み（「詰み」になる組み合わせは0件）。テンションは9th/b9のみ窓内到達可能で11th以上は物理的に到達不可能と判明したため、今回は対象から除外
- `js/audio.js` の `_chordVoices`（ポリフォニック再生）は既に可変長配列に対応済みと確認、修正不要
- `index.html` のホーム画面説明文・ハンバーガーメニューのバージョン表示、`guide.html` のコードトーン編セクション、`README.md` を更新
- `sw.js` を `fretboard-v14` に更新

---

## v1.5.3 — 2026-07-01

### ハンバーガーメニューが画面高さに収まらない問題を修正
- **問題**: `.menu-panel` は `height: 100%` の固定パネルだが、内部の `.menu-inner` に `overflow` 指定がなく、画面が低い端末（横持ちで高さが狭い端末など）ではメニュー下部の項目（波形選択・使い方ガイド・バージョン表示など）が画面からはみ出して見えず、スクロールもできなかった
- **修正**: `.menu-header`（「設定」タイトル＋✕ボタン）を `.menu-inner` の外に出して常に上部固定表示にし、`.menu-inner` 側に `flex: 1; min-height: 0; overflow-y: auto; -webkit-overflow-scrolling: touch;` を設定して本体だけを縦スクロール可能にした（`index.html` / `css/style.css`）。あわせて `.menu-panel` に safe-area 対応のpadding-top/bottomを追加
- パネル高さを一時的に縮小した状態でオーバーフロー発生とスクロール動作を実機ブラウザで確認済み。通常サイズでのメニュー表示・各設定項目の動作に変化がないことも確認
- ハンバーガーメニューのバージョン表示を `v1.5.3` に更新
- `sw.js` を `fretboard-v13` に更新

---

## v1.5.2 — 2026-07-01

### バージョン表示を追加・guide.htmlの記述を補完
- **ハンバーガーメニューにバージョン番号を追加**: `index.html` の「使い方ガイド →」リンクの下に `.app-version`（例:「v1.5.1」）を表示。今後バージョンを上げるたびにこの表示と `log.md` を同時に更新する運用ルールを `CLAUDE.md`「バージョン表示」セクションに明記
- **`guide.html` にコードトーン編のタップフィードバック色の説明を追加**: v1.5.1で追加した中立色（既に見つけ済みの構成音を再タップした際の青系表示）について、実装との齟齬を防ぐため「緑＝新規正解・赤＝構成音でない・青＝既発見の再タップ（進捗に影響なし）」を明記
- `README.md`「PWA 対応」にバージョン表示の記載を追加
- `sw.js` を `fretboard-v12` に更新

---

## v1.5.1 — 2026-07-01

### コードトーン編 Stage 0 のコードレビュー指摘を修正
コードレビュー（`CODE_REVIEW_2026-07-01_chordgame-stage0.md`）で指摘された4点に対応。

- **`_confirmedRoot` のモード間リークを修正**: インターバル編でRootを正解タップした後、度数を答える前にホームボタンで離脱してコードトーン編を開始すると、オレンジのルート確定マーカーが指板上に残留するバグを修正。`ChordGame._nextChord()` の先頭で `fretboard.clearConfirmedRoot()` を呼ぶよう追加
- **`stop()` でコード再生中の音を即カット**: `AudioEngine` に `stopChord()`（`_chordVoices` をフェードアウトして null 化）を追加し、`ChordGame.stop()` から呼ぶことで、ホームボタン離脱時にコード音（最大約1.2秒）が鳴り続ける問題を解消
- **フッタータイマー表示を仕様（タイマーなし）に合わせて非表示化**: `app.js` の `startChordPractice()` で `elTimer` に `hidden` クラスを付与し `startTimerDisplay()` の呼び出しを削除。インターバル編開始時（`startGame()`）は `hidden` を解除して従来通り表示
- **クリア済み構成音の再タップを中立色でフィードバック**: 既に消し込んだ度数を別ポジションで再タップした際、緑（正解）ではなく中立色（`COLOR.neutral` の青系）で表示するよう修正。`Fretboard.showFeedback()` の第3引数に `'neutral'` 状態を追加（`true`/`false` の既存呼び出し元＝インターバル編には影響なし）
- 上記4点はローカルサーバーでの実機ブラウザ確認済み。あわせて既存インターバル編（LV.1の複数問プレイ・タイマー動作・正誤フィードバック）への影響なしも確認
- **Stage 1への申し送り（今回未対応）**: `ChordGame` には出題可解性の検証（`Game._hasValidAnswer` 相当）がない。現状（ルートC固定・低音3弦・maj/minのみ）は手計算上すべて可解だが、ルートやコードタイプを増やすと表示窓内に構成音が収まらない「詰み」が発生し得るため、Stage 1着手時に必ず対応すること
- `sw.js` を `fretboard-v11` に更新（`js/chordGame.js` / `js/audio.js` / `js/app.js` / `js/fretboard.js` の内容変更をキャッシュに反映）

---

## v1.5.0 — 2026-07-01

### コードトーン編 Stage 0 を新規実装
- **新規 `js/chordGame.js`（`ChordGame` クラス）**: 「未クリア集合」方式の判定ロジックを実装
  - ルート C 固定、メジャー / マイナートライアド（`CHORD_TYPES.maj` / `.min`）のみ、低音3弦（6〜4弦）固定、練習モードのみ（タイマーなし・無限ループ）
  - コード開始時に構成音のピッチクラス集合を保持し、順不同のタップで発見した度数を集合から削除。空になったら 800ms 後に次のコードへ自動遷移
  - 進捗は常時テキスト表示（例:「R ✓　m3 ✓　5th（未）」）。ヒントや表示補助のON/OFFはStage 0の対象外
- **`js/audio.js` のポリフォニック再生バグを修正**: 既存の `playChord(midis, duration)` は内部で `playNote()` を呼んでいたため、単音再生用の後勝ちカット処理（`_activeVoice`）が働いて事実上モノフォニックになっていた（同時に複数音を鳴らし続けられなかった）。単音用ボイス生成処理を `_createVoice()` に共通化した上で、コード再生専用の独立管理（`_chordVoices`）を新設し、複数 `OscillatorNode` を同時に鳴らし続けられるよう修正
- **`index.html` / `js/app.js`**: ホーム画面にコードトーン編セクションを追加し、モード切り替え（インターバル編 ⇄ コードトーン編の表示トグル）を実装。準備中アラートを削除
- **`guide.html` にコードトーン編（Stage 0）の説明セクションを追加**: 遊び方（コードを聴く→構成音を順不同でタップ→次のコードへ）、Stage 0 の対象範囲（練習モードのみ・ルートC固定・メジャー/マイナートライアドのみ・低音3弦）を明記。既存のインターバル編の記述は変更なし
- `sw.js` を `fretboard-v10` に更新、`./js/chordGame.js` をキャッシュ対象に追加
- ローカルサーバーでの実機ブラウザ確認済み（未クリア集合の正解/不正解判定、進捗テキスト更新、コード自動遷移、既存インターバル編〔LV.1の10問完走・スコア保存・ランキング・練習モード・ヒント機能・メニュー設定〕への影響なしを確認）。GitHub Pages への公開は未実施

---

## v1.4.3 — 2026-07-01

### ヒントドットの不透明度を調整
- 最大不透明度を `0.7` → `0.5` に変更（`COLOR.hint`、`js/fretboard.js`）。サイズ調整（v1.4.2）と合わせて、控えめだが視認できるバランスに

---

## v1.4.2 — 2026-07-01

### ヒントドットのサイズを再調整
- v1.4.1 で半径スケールを `0.8` に拡大したところ大きすぎたため、正解フィードバックドットと同じ `0.32` に調整（`js/fretboard.js`）
- `guide.html` の「大きめの丸」という表現を削除

---

## v1.4.1 — 2026-07-01

### ヒントドットの視認性改善
- 実機（iPad）で確認したところ、ヒントドットの半径 `0.2`・最大不透明度 `0.7` が控えめすぎてほぼ気づけなかったため、半径スケールを `0.2` → `0.8` に拡大（`js/fretboard.js` `_drawCircle()` 呼び出し）。候補ポジションが近接している場合は隣接するドット同士が重なる可能性があるが、実機確認の範囲では視認性を優先
- `guide.html` の表現を「うっすら」→「半透明の大きめの丸」に修正

---

## v1.4.0 — 2026-07-01

### バグ修正・ヒント表示機能を追加
- **ハンバーガーメニュー表示バグの修正**: メニューを開いたままLVボタンをタップするとメニューが開いたままゲーム画面に遷移し、画面の一部が隠れる不具合を修正。`startGame()` の先頭で `menuPanel` の `open` クラスを外すよう変更（`js/app.js`）
- **詰まった時のヒント表示機能を追加**（LV.Max以外の全LV・練習モード対象）:
  - フェーズ1（Root待ち）: 出題音再生完了直後から5〜7秒（ランダム）無反応でタイムアウトし、表示範囲内・判定対象弦の中でルートのピッチクラスに一致する全ポジションに半透明ドットをフェードイン表示
  - フェーズ2（度数音待ち）: Rootタップ確定直後から5〜7秒無反応でタイムアウトし、同様にターゲット音の全ポジションを表示
  - ヒントの使用有無はスコア・ランキングに一切影響しない（記録しない）
  - `js/fretboard.js`: `showHints()` / `clearHints()` を追加。`requestAnimationFrame` で約400msかけてフェードイン。`_drawCircle()` に半径スケール・不透明度の引数を追加（既存呼び出しは後方互換）
  - `js/game.js`: `_scheduleHint()` でスケジュール時点のフェーズを記憶し、フェーズが変わっていたら発火時に無視（「もう一度」で度数フェーズ中に音を聞き直した際の誤発火防止）。ルート正解時・度数正解時・新しい問題開始時・ゲーム停止時にヒントとタイマーをクリア
  - ハンバーガーメニューに「ヒント」ON/OFFトグルを追加（デフォルトON、`js/app.js` / `index.html`）
- `guide.html` にヒント機能の説明を追記
- **練習モードの次問移行タイマーを統一**: 練習モードのみ正解後の次問移行が `800ms`（通常LVは `500ms`）となっており、`guide.html` の「0.5秒後に自動で次の問題へ進みます」という記述と食い違っていた。意図的な設計差ではなかったため、練習モードも `500ms` に統一（`js/game.js`）

---

## v1.3.1 — 2026-07-01

### ドキュメント整合性の修正
- **コードトーン編の予定バージョン表記を統一**: `README.md`・`CLAUDE.md` が「v1.1 予定」のままだったのに対し、`log.md` の TODO は「v1.5予定」となっており内部矛盾していた（v1.3.0まで進んでも未着手のまま放置されていた）。実態（未着手・保留機能）に合わせて `README.md` / `CLAUDE.md` を「v1.5 予定・未着手」に統一

---

## v1.3.0 — 2026-07-01

### ランキング単独閲覧機能を追加
- **ホーム画面に🏆ボタンを追加**: 各LVの行に「ランキングを見る」ボタン（`.lv-rank-btn`）を追加し、プレイせずにそのLVの保存済みランキングだけを確認できるように（`js/app.js` `showRanking()`）
- リザルト画面をプレイ後の「結果」表示とランキング単独閲覧の「ランキング」表示で共用し、タイトル・スコア/タイム表示・ボタン文言（「もう一度」⇄「プレイする」）を切り替え
- `index.html` の LV リストを `<li>` でラップする構造に変更（`css/style.css` に `.lv-row` / `.lv-rank-btn` を追加）
- `guide.html`「スコアはどこに保存される？」に、練習モードにはスコア記録・リザルト画面が存在しない旨を追記

---

## v1.2.3 — 2026-07-01

### バグ修正・ガイド文言修正
- **タイマー加算漏れの修正**: `Game._startTimer()` が呼ばれるたびに経過時間を加算せず上書きしていたため、「もう一度」ボタンで出題音を聞き直すと直前までの経過時間が消えていた（実質タイムのリセット扱いになっていた）。前回開始時刻からの経過分を `_totalTime` に加算してから計り直すよう修正（`js/game.js`）
- **`guide.html` を実装と整合**:
  - タイマーの説明を「画面表示のタイマー（ゲーム画面に入ってから止まらず動き続ける経過時間）」と「ランキング保存用タイム（出題音再生後から正解までを10問分合計、別計算）」が別物である旨に修正
  - マスク弦の説明を「判定弦（灰色がかっている弦）は対象外」という矛盾した表現から「暗く塗りつぶされている弦（マスクされた弦）が対象外、暖色ハイライトの弦がタップ可能」に修正（前回の指板視認性改善に合わせた更新）
  - 開放弦除外の説明を実際のUI（「開放弦」ボタンの表示が「除外」になっている状態）に合わせて修正

---

## v1.2.2 — 2026-07-01

### 音再生の調整
- **発音時間を全体的に延長**: タップフィードバック音 `0.5秒→0.7秒`、出題ルート音 `0.6秒→0.8秒`、出題度数音 `0.8秒→1.0秒`。ルート→度数音の再生間隔も `700ms→800ms` に調整
- **後勝ちのモノフォニック再生**: `AudioEngine.playNote()` に直前の音を即座にフェードアウト（20ms）して停止する処理を追加。連続タップ時は後からタップされた音が優先して鳴るように変更（`js/audio.js`）

---

## v1.2.1 — 2026-07-01

### 指板表示の視認性改善
- **フレット番号とマスクの重なり解消**: `_drawLayer2` のマスク描画を盤面エリア（`PAD_TOP`〜`PAD_TOP+boardH`）にクリップし、最上段弦のマスク帯がフレット番号ラベルの余白にはみ出さないよう修正
- **マスクのコントラスト強化**: `maskStr` の不透明度を `0.55` → `0.78` に引き上げ、判定対象外の弦をより明確に暗転
- **判定対象弦のハイライト追加**: `activeStr`（暖色系の半透明帯）を新設し、タップ可能な弦の帯を視覚的に強調

---

## v1.2.0 — 2026-06-30

### 説明ページ追加
- **`guide.html` 新規作成**: 初めて触る人向けのスタンドアロン説明ページ
  - ホーム画面への追加手順（iOS Safari）、コンセプト、遊び方、正解判定ルール、難易度一覧
  - FAQ アコーディオン（`<details>` 要素）
  - Licketch ヘルプ画面と同構成・同トーン（ダークテーマ）
- `index.html` のハンバーガーメニューに「使い方ガイド →」リンクを追加
- `css/style.css` に `.menu-link` スタイルを追加
- `sw.js` の ASSETS に `./guide.html` を追加（`fretboard-v9` に更新）
- **公開URL**: `https://monsieurnoir.github.io/guitar-fretboard-app/guide.html`

---

## v1.1.1 — 2026-06-30

### セキュリティ・バグ修正（コードレビュー対応）

**セキュリティ修正**
- **`server.js` Path Traversal 対策**:
  - `ROOT_WITH_SEP` でセパレータ付き比較（ディレクトリ名プレフィックス誤許可を修正）
  - `decodeURIComponent` でURLエンコードをデコード後に検証
  - NULバイト（`\0`）を含むパスを 400 で拒否
  - `fs.realpath` でシンボリックリンク先を検証

**ゲームロジック修正**
- **`_hasValidAnswer` の走査範囲修正**: 指板全体（0〜MAX_FRET）→ `calcDisplayRange` の表示窓内のみに限定（練習モードで表示窓外の正解を誤検出していたバグを修正）
- **練習モード `judgeStrings` 拡張**: `[0]`（6弦のみ）→ `[0,1,2]`（低音3弦）に変更（5th=G が1弦の表示窓に入らず詰むケースを解消）
- **タイマー管理強化**: `stop()` で `_playTimer` と `_nextQuestionTimer` の両方をキャンセル
- **`_nextQuestion` ハンドル保存**: `setTimeout` の戻り値を `this._nextQuestionTimer` に保存

**その他修正**
- **localStorage guard**: `loadScores()` / `saveScore()` を `try/catch` で包み、iOS Safari プライベートブラウズの `QuotaExceededError` を無音処理
- **touchend/click 二重発火防止**: `suppressNextClick` フラグでタップ直後の click イベントを抑制
- **縦余白タップ誤検出防止**: `_handleTap` で Canvas 上下余白領域のタップを無視

---

## v1.1.0 — 2026-06-30

### 機能拡張・バグ修正
- **17F対応**: 表示・タップ判定・出題バリデーションを 0F〜17F に統一
  - `MAX_FRET = 17`（旧: 16）
  - `calcDisplayRange` の上限を `MAX_FRET-1 = 16` に修正（最大表示ゾーン = 17F、18F 表示バグを解消）
  - ポジションマークに 17F を追加
- **基準音バリエーション拡張（LV.4 / LV.Max）**: `rootStrings: [0,1,2]`・`rootOctaves: [0,1]` で 6〜4弦 × 0/1 オクターブからランダム選択
- **出題バリデーション**: `_hasValidAnswer()` + 最大 30 回リトライで「18F 以上にしか正解がない」問題を排除
- **開放弦対応**
  - ナット左側に 0F 専用ゾーンを描画、タップ可能
  - フレット番号を `0, 1, 2, 3...` 形式で表示
  - 開放弦を回答対象から除外できる設定をハンバーガーメニューに追加
- **波形デフォルト変更**: `triangle` → `square`
- **2タップ方式**: フェーズ1（Root タップ）→ フェーズ2（度数音タップ）の順を強制
- **縦持ち対応**: CSS rotate 削除、「横向きにしてください」オーバーレイ方式に変更
- **PWA**: Service Worker を `fretboard-v8` に更新

---

## v1.0.0 — 2026-06-29

### 初期実装
- インターバル編: LV.1〜LV.Max + 練習モード
- 指板Canvas描画（横持ち、左ヘッド/右ブリッジ、6フレット幅動的表示）
- Web Audio API OscillatorNode音再生（sine/triangle/sawtooth/square切り替え）
- ルートからの半音数によるピッチクラス判定 + テンション判定
- スコアランキング（モード×LVごとにlocalStorageに上位20件保存）
- PWA対応（manifest.json + Service Worker）

---

## 技術負債・TODO

- [ ] コードトーン編 Stage 1以降（7th・テンション、出題度数の部分集合、対象弦の可変設定、出題順序制約）
- [ ] 指板幅の可変設定（v1.5予定）
- [ ] アイコン画像の本番版作成
- [ ] ハプティクスフィードバック（保留）
- [ ] CAGED システム表示（v1.5予定）
- [ ] カスタム出題設定（v1.5予定）
